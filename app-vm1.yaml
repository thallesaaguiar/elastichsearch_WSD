apiVersion: v1
kind: Pod
metadata:
  name: app-vm1
spec:
  containers:
  - name: app-vm1
    image: ubuntu:latest
    command: ["/bin/bash", "-c", "--"]
    args: ["while true; do sleep 30; done;"]
    ports:
    - containerPort: 80
